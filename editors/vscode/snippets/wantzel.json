{
  "main": {
    "prefix": "main",
    "body": [
      "include \"io.wz\";",
      "",
      "var",
      "  ${1:n}: int;",
      "",
      "begin",
      "  $0",
      "end."
    ],
    "description": "A whole program: the smallest thing that compiles and runs"
  },
  "procedure": {
    "prefix": "procedure",
    "body": [
      "procedure ${1:name}(${2:a: int});",
      "begin",
      "  $0",
      "end;"
    ],
    "description": "A procedure (returns nothing)"
  },
  "function": {
    "prefix": "function",
    "body": [
      "function ${1:name}(${2:a: int}): ${3:int};",
      "begin",
      "  $0",
      "  return ${4:0};",
      "end;"
    ],
    "description": "A function; every path must return, and the caller may not discard the result"
  },
  "schema": {
    "prefix": "schema",
    "body": [
      "include \"json.wz\";",
      "",
      "schema ${1:Name} = record",
      "  ${2:field}: ${3:int};",
      "  ${4:optional}: text[${5:64}]?;",
      "end;"
    ],
    "description": "A JSON schema: gives <Name>.parse, <Name>.write, <Name>.clear and <Name>.jsonschema"
  },
  "tools": {
    "prefix": "tools",
    "body": [
      "include \"json.wz\";",
      "",
      "schema ${1:Args} = record",
      "  ${2:a}: int \"${3:what it means}\";",
      "end;",
      "",
      "schema ${4:Result} = record",
      "  ${5:sum}: int;",
      "end;",
      "",
      "tools",
      "  ${6:name}(${1:Args}): ${4:Result} \"${7:What the tool does.}\" readonly;",
      "end;",
      "",
      "include \"tools.wz\";",
      "",
      "function tool.${6:name}(a: array of ${1:Args}; r: array of ${4:Result}): int;",
      "begin",
      "  r[0].${5:sum} := a[0].${2:a};",
      "  return 0;",
      "end;",
      "",
      "// lib/tools.wz brings in http.wz, so even a stdio-only program needs this",
      "procedure app.request;",
      "begin",
      "end;"
    ],
    "description": "A tool table: the compiler generates tools/list, the argument parsing and the dispatch"
  },
  "for": {
    "prefix": "for",
    "body": [
      "for ${1:i} := ${2:0} to ${3:n} - 1 do",
      "begin",
      "  $0",
      "end;"
    ],
    "description": "A counted loop"
  },
  "while": {
    "prefix": "while",
    "body": [
      "while ${1:condition} do",
      "begin",
      "  $0",
      "end;"
    ],
    "description": "A loop with the test at the top"
  },
  "buffer": {
    "prefix": "buffer",
    "body": [
      "  ${1:buf}: array[0..${2:255}] of char;",
      "  ${1:buf}_n: int;"
    ],
    "description": "A buffer with its length beside it, inside a var block -- fill it with ${1:buf}_n := io.push(${1:buf}, 0, \"text\"); every memory error in the log came from not keeping the two together"
  },
  "readfile": {
    "prefix": "readfile",
    "body": [
      "fd := sys3(SYS.open, addr(${1:path}[0]), O_RDONLY, 0);",
      "if fd < 0 then begin io.puts(STDERR, \"cannot open the file\\n\"); halt(1); end;",
      "n := io.read(fd, addr(${2:buf}[0]), len(${2:buf}));",
      "sys1(SYS.close, fd);"
    ],
    "description": "Open a file, read it into a buffer, close it -- with the failure tested"
  },
  "jsontext": {
    "prefix": "jsontext",
    "body": [
      "// the decoded text of a JSON value: kv.text strips the quotes and the escapes,",
      "// and refuses (-1) when the value is not a string",
      "if kv.find(${1:buf}, 0, ${2:n}, ${3:keybuf}[0..${4:keyn} - 1]) then",
      "begin",
      "  ${5:textn} := kv.text(${1:buf}, ${6:dst}, 0);",
      "  if ${5:textn} < 0 then $0                 // not a string, or it does not fit",
      "end;"
    ],
    "description": "Read a text value out of JSON -- reading kv.vat..kv.vend directly gives it back WITH its quotes, which compares unequal to the same word without them, silently (logged 11-09-2026)"
  },
  "growing": {
    "prefix": "growing",
    "body": [
      "// a list of unknown length without a heap: a fixed array with its count, and the",
      "// bound tested BEFORE the write -- the write that does not fit is the one that",
      "// corrupts the next thing",
      "var",
      "  ${1:items}: array[0..${2:255}] of ${3:int};",
      "  ${1:items}_n: int;",
      "",
      "if ${1:items}_n >= len(${1:items}) then",
      "begin",
      "  io.puts(STDERR, \"${4:too many items}\\n\");",
      "  $0",
      "end",
      "else",
      "begin",
      "  ${1:items}[${1:items}_n] := ${5:value};",
      "  ${1:items}_n := ${1:items}_n + 1;",
      "end;"
    ],
    "description": "Collect an unknown number of things, with the bound checked before the write"
  },
  "mapfile": {
    "prefix": "mapfile",
    "body": [
      "// map a whole file read-only: the kernel pages in what you touch and reclaims it,",
      "// so memory stays flat whatever the file size",
      "if not fs.stat(addr(${1:path}[0])) then begin io.puts(STDERR, \"cannot stat\\n\"); halt(1); end;",
      "${2:size} := fs.size;",
      "${3:fd} := sys3(SYS.open, addr(${1:path}[0]), O_RDONLY, 0);",
      "if ${3:fd} < 0 then begin io.puts(STDERR, \"cannot open\\n\"); halt(1); end;",
      "${4:base} := sys6(SYS.mmap, 0, ${2:size}, PROT_READ, MAP_SHARED, ${3:fd}, 0);",
      "sys1(SYS.close, ${3:fd});                   // the mapping keeps its own reference",
      "if ${4:base} < 0 then begin io.puts(STDERR, \"cannot map\\n\"); halt(1); end;",
      "$0",
      "sys2(SYS.munmap, ${4:base}, ${2:size});"
    ],
    "description": "Map a file and walk it as an ordinary array through view(base, size)"
  },
  "checkwrite": {
    "prefix": "checkwrite",
    "body": [
      "// a generated write REFUSES with -1 rather than truncating: half a JSON object is",
      "// not a shorter object but a syntax error, so the result must be tested",
      "${1:n} := ${2:Reply}.write(${3:dst}, 0, ${4:rec}, ${5:src});",
      "if ${1:n} < 0 then",
      "begin",
      "  $0                                  // report it; do not send a half object",
      "end;"
    ],
    "description": "Test the result of a generated write -- unlike io.push, it refuses instead of truncating"
  }
}
