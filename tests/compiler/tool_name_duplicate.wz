// Must not compile: "tool_name_duplicate.wz:14: tool name "wordcount" is already used by
// the tool on line 13; choose another name" -- two tools in the same block with the same
// name. Before this it surfaced as "<tools>:N: duplicate declaration", a line of the
// GENERATED text that names neither entry.
import json;
type A = schema
  x: int;
end;
type R = schema
  y: int;
end;
tools
  wordcount(A): R "first";
  wordcount(A): R "second";
end;
import toolsmcp;
function tool.wordcount(a: array of A; r: array of R): int;
begin
  return 0;
end;
begin
end.
