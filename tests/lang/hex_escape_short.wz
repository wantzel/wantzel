// Must not compile: one hex digit is not two. "\x4" ends the escape at the closing quote,
// which is exactly the case a variable-length escape would have accepted silently.
import io;
var s: str;
begin
  s := "\x4";
  io.puts(STDOUT, s);
end.
