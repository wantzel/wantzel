---
name: Bug report
about: Something the compiler, the library or the documentation gets wrong
labels: bug
---

**What happened, and what you expected instead**

<!-- Paste the exact message. Compiler errors look like `wantzel: file.wz:12: ...` and
     runtime errors like `runtime error: ... at file.wz:42` — both name the line. -->

**A `.wz` file that reproduces it**

<!-- As small as you can get it. If cutting it down makes the problem disappear, say so:
     that is a useful clue in itself. -->

```pascal

```

**Version** — the output of `wantzel --version`:

```

```

**Platform** — the Linux distribution and kernel, if it seems to matter:
